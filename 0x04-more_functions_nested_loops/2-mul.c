#include "main.h"
/**
 *add - Makes the multiplication of two numbers
 *@a: First operand
 *@b: Second operand
 *
 * Return: The multiplication of the two parameters
 */
int mul(int a, int b)
{
	return (a * b);
}
